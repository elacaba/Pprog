

#include <stdlib.h>
#inlcude <stdio.h>

person *read_person(FILE *f);


/*
  Returns the random score and puts it in last_score
*/
int play(person *p, int skill){
}


int get_last(person *p){
  if(p==NULL)
   return -1;
  return person.last_score;
    }

void kill_kill_kill(person *p);
